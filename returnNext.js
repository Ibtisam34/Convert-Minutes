function getNextNumber(num) {
    return num + 1;
  }
  // Testing the function
  const currentNumber = 5;
  const nextNumber = getNextNumber(currentNumber);
  console.log(`The next number after ${currentNumber} is ${nextNumber}.`);